@model LabelIndexViewModel<Plato.Ideas.Labels.Models.Label>
@{
    Layout = null;
}

@await DisplayAsync(new View("IdeasGetLabelList", new
{
    options = @Model.Options,
    pager = @Model.Pager
}))

