@model EditTopicChannelsViewModel

<card>
    <card-body>

        @await DisplayAsync(new View("ChannelsDropDown", new
        {
            selectedChannels = @Model.SelectedChannels,
            htmlName = @Model.HtmlName
        }))
    
        
        @await DisplayAsync(new View("ChannelsSelect", new
        {
            selectedChannels = @Model.SelectedChannels,
            htmlName = @Model.HtmlName
        }))


    </card-body>
</card>
