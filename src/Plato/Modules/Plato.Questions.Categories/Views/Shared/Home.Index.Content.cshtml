@model CategoryIndexViewModel

@await DisplayAsync(new View("QuestionCategoryList", new
{
    options = @Model.Options
}))

@if (Model.Options.CategoryId != 0)
{
    @await DisplayAsync(new View("QuestionList", new
    {
        options = @Model.EntityIndexOptions,
        pager = @Model.Pager
    }))
}

