@model CategoryBase

@if (Model != null)
{
    <a asp-route-area="Plato.Questions" asp-route-controller="Home" asp-route-action="Create" asp-route-channel="@Model.Id" class="btn btn-primary" title="Reply">
        <i class="fal fa-fw fa-plus"></i>
        @T["New Question"]
    </a>
}
else
{
    <a asp-route-area="Plato.Questions" asp-route-controller="Home" asp-route-action="Create" class="btn btn-primary" title="Reply">
        <i class="fal fa-fw fa-plus"></i>
        @T["New Question"]
    </a>
}
