@using Microsoft.AspNetCore.Routing
@using Plato.Internal.Data.Abstractions
@model UserIndexViewModel
@{
    Layout = null;
}
@await DisplayAsync(new View("GetUserList", new
{
    options = @Model.Options,
    pager = @Model.Pager
}))

