@model UsersPagedViewModel

<h1>User List 2</h1>

<ul class="list-group">
    @foreach (var user in Model.Results.Data)
    {
        @await DisplayAsync(new View("User.ListItem", user))
    }
</ul>

<pager model="@Model.PagerOpts"></pager>


