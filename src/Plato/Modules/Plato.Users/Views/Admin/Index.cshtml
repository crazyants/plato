@model UsersPaged
@{
    ViewData["Title"] = "Manage Users";
}

<h4>@T["Manage Users"]</h4>
<span class="text-muted">@T["Manage your registered users"]</span>


<ul class="list-group">
    @foreach (var user in Model.PagedResults.Data)
    {
        <li class="list-group-item">
           @user.UserName
        </li>
    }
</ul>



