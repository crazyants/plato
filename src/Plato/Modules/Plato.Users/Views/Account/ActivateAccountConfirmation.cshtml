@model ForgotPasswordViewModel
@{
    ViewData["Title"] = T["Activate Account"];
}

<h2>@T["Success"]</h2>
<div class="row">
    <div class="col-md-6">
        <p>
            @T["Your account has been activated."] <a asp-route-area="Plato.Users" asp-controller="Account" asp-route-action="Login">@T["Log in"]</a>
        </p> 
    </div>
</div>
