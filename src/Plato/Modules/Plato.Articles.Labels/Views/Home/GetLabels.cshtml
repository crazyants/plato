@model LabelIndexViewModel<Plato.Articles.Labels.Models.Label>
@{
    Layout = null;
}

@await DisplayAsync(new View("GetArticleLabelList", new
{
    options = @Model.Options,
    pager = @Model.Pager
}))

