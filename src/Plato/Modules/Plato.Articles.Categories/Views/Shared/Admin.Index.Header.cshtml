@model CategoryBase

@if (@Model.Id == 0)
{
    <h4>@T["Channels"]</h4>
}
else
{
    <h4>@Model.Name</h4>
}

<span class="text-muted">@T["Channels help group & organize conversations."]</span>
<hr />


