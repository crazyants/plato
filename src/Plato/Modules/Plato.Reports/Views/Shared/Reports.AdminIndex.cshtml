@using Plato.Internal.Models.Extensions
@model ReportOptions

@await DisplayAsync(new View("ViewsByDateChart", new
{
    options = @Model
}))

@await DisplayAsync(new View("ViewsByFeatureList", new
{
    options = @Model
}))
