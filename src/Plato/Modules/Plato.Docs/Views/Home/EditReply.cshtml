@model LayoutViewModel
@{
    AddTitleSegment(T["Docs"]);
    AddTitleSegment(T["Edit Comment"]);
}

<form asp-controller="Home" asp-action="EditReply" method="post" enctype="multipart/form-data" data-disable-submit="true" role="form">
    <div class="layout">
        <div class="layout-header">
            @if (Model.Alerts != null)
            {
                <div class="layout-alerts">
                    <view model="@Model.Alerts"></view>
                </div>
            }
            @if (Model.Tools != null)
            {
                <div class="float-right layout-tools">
                    <view model="@Model.Tools" />
                </div>
            }
            @if (Model.Header != null)
            {
                <div class="layout-header-item">
                    <view model="@Model.Header" />
                </div>
            }
            @if (Model.Meta != null)
            {
                <div class="layout-meta">
                    <view model="@Model.Meta"></view>
                </div>
            }
        </div>
        @if (Model.SideBar != null)
        {
            <div class="row gutter-sm layout-content">
                <div class="col-lg-9">
                    @if (Model.Content != null)
                    {
                        <view model="@Model.Content"></view>
                    }
                    @if (Model.Actions != null || Model.Footer != null)
                    {
                        <div class="layout-footer">

                            <div class="list-left"></div>
                            <div class="list-body">
                                <card>
                                    <card-body>
                                        @if (Model.Actions != null)
                                        {
                                            <div class="float-right layout-actions">
                                                <view model="@Model.Actions" />
                                            </div>
                                        }
                                        @if (Model.Footer != null)
                                        {
                                            <div class="layout-footer-item">
                                                <view model="@Model.Footer"></view>
                                            </div>
                                        }
                                    </card-body>
                                </card>
                            </div>
                        </div>
                    }
                </div>
                <div class="col-lg-3">
                    @if (Model.SideBar != null)
                    {
                        <view model="@Model.SideBar"></view>
                    }
                </div>
            </div>
        }
        else
        {
            <div class="layout-content">
                @if (Model.Content != null)
                {
                    <view model="@Model.Content"></view>
                }
                @if (Model.Actions != null || Model.Footer != null)
                {
                    <div class="list-left"></div>
                    <div class="list-body">
                        <card>
                            <card-body>
                                <div class="layout-footer">
                                    @if (Model.Actions != null)
                                    {
                                        <div class="float-right layout-actions">
                                            <view model="@Model.Actions" />
                                        </div>
                                    }
                                    @if (Model.Footer != null)
                                    {
                                        <div class="layout-footer-item">
                                            <view model="@Model.Footer"></view>
                                        </div>
                                    }
                                </div>
                            </card-body>
                        </card>
                    </div>
                }
            </div>
        }
    </div>
</form>