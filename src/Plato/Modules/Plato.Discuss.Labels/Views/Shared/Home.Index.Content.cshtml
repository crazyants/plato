@model LabelIndexViewModel

@await DisplayAsync(new View("LabelList", new
{
    options = @Model.Options,
    pager = @Model.Pager
}))



@*<card class="card card-body-flush">
        <card-title>
            <h6 class="card-title">@T["All Labels"]</h6>
        </card-title>
        <card-body>

            @if (Model.Labels != null)
            {
                <div class="list-group list-group-flush list-group-hover">
                    <div class="row">
                        @foreach (var label in Model.Labels)
                        {

                                <a asp-route-area="Plato.Discuss.Labels" asp-route-controller="Home" asp-route-action="Display" asp-route-id="@label.Id" asp-route-alias="@label.Alias" title="@label.Description" class="list-group-item list-group-item-action col-lg-3 col-md-6 col-sm-12">
                                    <span class="btn btn-sm font-weight-bold" style="background-color: @label.BackColor; color: @label.ForeColor">
                                        @label.Name
                                    </span>
                                </a>

                        }
                    </div>
                </div>
            }
            else
            {
                <ul class="list-group list-group-flush list-group-hover">
                    <li class="list-group-item">
                        @T["No labels found"]
                    </li>
                </ul>
            }

        </card-body>
    </card>*@

