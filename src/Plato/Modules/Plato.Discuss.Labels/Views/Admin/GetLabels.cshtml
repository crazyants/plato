@model LabelIndexViewModel<Plato.Discuss.Labels.Models.Label>
@{
    Layout = null;
}

@await DisplayAsync(new View("DiscussGetLabelList", new
{
    options = @Model.Options,
    pager = @Model.Pager
}))

