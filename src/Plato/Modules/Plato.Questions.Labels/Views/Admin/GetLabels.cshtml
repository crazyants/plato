@model LabelIndexViewModel<Plato.Questions.Labels.Models.Label>
@{
    Layout = null;
}

@await DisplayAsync(new View("QuestionsGetLabelList", new
{
    options = @Model.Options,
    pager = @Model.Pager
}))

