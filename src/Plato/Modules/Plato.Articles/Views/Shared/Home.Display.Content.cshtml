@model EntityViewModel<Article, Comment>

@await DisplayAsync(new View("Article", new
{
    options = @Model.Options
}))

@await DisplayAsync(new View("ArticleCommentList", new
{
    options = @Model.Options,
    pager = @Model.Pager
}))


