@model EditRoleViewModel

<button type="submit" asp-controller="Admin" asp-action="Delete" asp-route-id="@Model.Id.ToString()" class="btn btn-danger btn-sm" itemprop="UnsafeUrl">@T["Delete Role"]</button>
