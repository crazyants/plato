@model EditRoleViewModel

@if (!Model.IsNewRole && !Model.IsDefaultRole)
{
<button type="submit" asp-controller="Admin" asp-action="Delete" asp-route-id="@Model.Id.ToString()" data-provide="confirm" class="btn btn-danger btn-sm" itemprop="UnsafeUrl">
    <i class="fal fa-trash"></i>
    @T["Delete Role"]
</button>
}

