@model SearchIndexViewModel


<card>
    <card-title>
        <h6 class="card-title">@T["Search"]</h6>
    </card-title>
    <card-body>
        @await DisplayAsync(new View("Search", new
        {
            value = Model.Keywords   
        }))
    </card-body>
</card>

