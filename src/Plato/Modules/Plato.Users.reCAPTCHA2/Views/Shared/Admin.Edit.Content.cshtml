@model ReCaptchaSettingsViewModel

<form asp-route-action="Index" method="post" enctype="multipart/form-data">

    <card>
        <card-title>
            <h6 class="card-title">@T["General"]</h6>
        </card-title>
        <card-body>

            <div class="row">
                <div class="col">
                    <fieldset>
                        <div class="form-group" asp-validation-class-for="DefaultFrom">
                            <label asp-for="@Model.SiteKey">
                                <strong>@T["Site Key"]</strong>
                                <span asp-validation-for="@Model.SiteKey" class="text-danger"></span>
                                <br />
                                <span class="text-muted">@T["The public reCAPTCHA Site Key provided by Google."]</span>
                            </label>
                            <input asp-for="@Model.SiteKey" class="form-control" autofocus />
                        </div>
                    </fieldset>
                </div>
            </div>

        </card-body>
    </card>

    <card class="card mt-4">
        <card-footer>
            <button class="btn btn-primary" type="submit">@T["Save"]</button>
        </card-footer>
    </card>

</form>