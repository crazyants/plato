@model EditEntityReplyViewModel

@if (Model.Id == 0)
{
    <h4>@T["Post Answer"]</h4>
    <span class="text-muted">@T["Post a new answer."]</span>
    <hr />
}
else
{
    <h4>@T["Edit Answer"]</h4>
    <span class="text-muted">@T["Edit an existing answer."]</span>
    <hr />
}



