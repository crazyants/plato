@model EntityViewModel<Question, Answer>

@await DisplayAsync(new View("Question", new
{
    options = @Model.Options
}))

@await DisplayAsync(new View("QuestionAnswerList", new
{
    options = @Model.Options,
    pager = @Model.Pager
}))


