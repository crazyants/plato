@model Question
@if (!Model.IsClosed)
{
    @if (this.CurrentUser != null)
     {
         <a asp-route-area="Plato.Users" asp-route-controller="Account" asp-route-action="Login" asp-route-returnUrl="@Context.Request.Path" data-provide="postAnswer" class="btn btn-primary text-uppercase">
             <i class="fal fa-fw fa-plus"></i>
             @T["Add Answer"]
         </a>
    }
     else
     {
         <a asp-route-area="Plato.Users" asp-route-controller="Account" asp-route-action="Login" asp-route-returnUrl="@Context.Request.Path" class="btn btn-primary text-uppercase" data-toggle="tooltip" title="@T["Login to post a comment"]">
             @T["Add Answer"]
         </a>
     }
 
}

