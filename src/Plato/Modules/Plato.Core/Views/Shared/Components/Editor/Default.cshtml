@model EditorViewModel

<textarea 
    id="@Model.Id" 
    name="@Model.HtmlName" 
    data-val-required="The message field is required"
    class="form-control md-textarea" 
    placeholder="@Model.PlaceHolderText" 
    asp-autofocus="@Model.AutoFocus"
    rows="@Model.Rows">@Model.Value</textarea>